<!-- record-meta
{
  "record_id": "20260921-062500-e8a37ad",
  "experiment": "timing-characterization",
  "supersedes": "20260909-225431-86f71d2",
  "git_revision": "e8a37ad641c7913495f8e9aa14d5dd4c1bf93d45",
  "framework_gap": "G4",
  "harness": "flow/sta-sweep.sh",
  "corner_count": 18,
  "runs_per_corner": 3,
  "provenance": {
    "klt_version": "0.5.0+g2b7caa9939af",
    "openroad_version": "26Q3-1278-g4421880472",
    "pdk": {
      "name": "sky130A",
      "version": "open_pdks c6d73a35f524070e85faff4a6a9eef49553ebc2b"
    },
    "inputs": [
      {
        "path": "layout/logic_tile.def",
        "role": "analysed geometry (klt sta) -- PDN routing added by issue #41",
        "content_hash": "sha256:2979e7fb3e292e20e31c85e642687b4cacdc7ff9f7d72c7bc2b526bb793badaf"
      },
      {
        "path": "layout/logic_tile.gds",
        "role": "parasitic-extraction source (klt extract --parasitics)",
        "content_hash": "sha256:38498092805ec6a6f23cf9e13f57a055f765412595b422b66cf3a24ae6722a7f"
      },
      {
        "path": "flow/build/sta/logic_tile.spef",
        "role": "extracted parasitics, name-rewritten and DATE-canonicalized (not committed -- regenerated by flow/sta-sweep.sh; this is the sanitized SPEF the 2026-09-18 PDN re-sweep annotated against under the recorded-lineage toolchain, and a regeneration under a different klt build hashes differently; every per-corner report independently pins the SPEF it was annotated with via its own spef_sha256 field)",
        "content_hash": "sha256:4986d8339d8d2b4d1b809d21feeb04855a00c50de85dcc7e9f124dc3420a9b2d"
      },
      {
        "path": "flow/sta-sweep.sh",
        "content_hash": "sha256:cb946e4572f33302aaadb6828c882ae302515ec9ea6d4ee73af6e61189356f62"
      },
      {
        "path": "flow/sta_sanitize_names.py",
        "content_hash": "sha256:200a4bfa84b55793c6827471b61ca574e59c0c4c5444bbb41d728e258b228e8f"
      },
      {
        "path": "flow/sta_report_trim.py",
        "content_hash": "sha256:78ab3e95b251de3f56a5d0d091bc428f6e53e1401d80939b21f50540d882ab9d"
      }
    ]
  }
}
-->

# Record 20260921-062500-e8a37ad

- **Record ID**: 20260921-062500-e8a37ad
- **Claim**: T1 framework gap G4 -- the 18-corner STA re-characterization of
  the **post-PDN** `logic_tile` geometry. Issue #41 added a real power
  delivery network to the layout (`flow/layout.sh`'s `power` block: met1
  0.48/5.44 followpin row rail, met4 1.6/27.14/13.57, met5 1.6/27.2/13.6
  straps, `sky130_fd_sc_hd__tapvpwrvgnd_1` tapcells and endcaps), which
  changes the parasitic-extracted net the SPEF describes, so the entire
  sweep was re-run against the new geometry. Regenerate with `./flow/
  sta-sweep.sh`; the harness, trims and per-corner reports are the same
  shape the superseded record documented.
- **What this supersedes**:
  `measurements/timing-characterization/records/20260909-225431-86f71d2.md`
  -- the pre-PDN 18-corner sweep. This record supersedes it because the
  inputs it pins changed legitimately (the DEF and GDS hashes moved with
  the PDN; the record itself is append-only and needs a successor, not an
  edit). Its RATIFIED acceptance criteria
  (`spec/decisions/0002-tile-timing-spec-ratification.md`) are unchanged
  and are re-met by the numbers below -- see "What this record does NOT
  claim".
- **Toolchain of the re-sweep** (2026-09-18): klt `0.5.0+g2b7caa9939af`
  (pinned by `layout/logic_tile.erc.json`'s own `provenance.klt_version`,
  written by the same session) and OpenROAD `26Q3-1278-g4421880472`
  (`flow/tool_versions.sh`'s recorded value; the operator-local
  `openroad` wrapper runs an unpinned `openroad/orfs:latest` image, which
  has since moved -- see "Re-verification" below).

## Result: setup/hold-clean at all 18 corners, post-PDN

Every figure below is read directly from the committed per-corner reports
under `measurements/timing-characterization/corners/` (which this sweep
regenerated and diff-checked):

- **Binding setup corner** (minimum SPEF-annotated `worst_slack_ns` across
  all 18 corners): **`ss_n40C_1v28`** -- WNS 15.465 ns (LEF-only) /
  **15.2146 ns (SPEF-annotated)**, `total_negative_slack_ns: 0`,
  0 setup violations, 0 hold violations, extrapolated `fmax_mhz` 208.967
  (SPEF-annotated).
- **Fastest corner**: `ff_n40C_1v95` -- WNS 19.5927 ns (SPEF-annotated),
  `fmax_mhz` 2455.04.
- All 18 corners: setup-violation count, hold-violation count and total
  negative slack are 0 in both the LEF-only and SPEF-annotated runs; every
  SPEF run reports `spef_annotation.annotation_complete: true`.
- **Fmax caveat carried forward** (per ADR-0002): `fmax_mhz` is a
  single-period `1/(T-WNS)` extrapolation, not a bisected measurement --

  the slack numbers above are the trustworthy ones.
- **Delta vs. the superseded record**: binding-corner SPEF WNS moved
  15.1760 -> 15.2146 ns (LEF-only unchanged at 15.465 ns). The PDN straps
  are net-new met4/met5 routing the router now budgets for; the margin
  moves by ~39 ps and stays comfortably clear of the 20 ns period. Same
  binding corner, same clean verdicts -- the ratified criteria of
  ADR-0002 hold unchanged.

## Re-verification (2026-09-21)

- `flow/layout.sh` regenerates the committed GDS and DEF **byte-identical**
  under the installed klt `0.5.0+g2b1e55e51bb8.dirty` (verified by the
  check-mode diffs of `flow/layout.sh`, `flow/lvs.sh`, `flow/drc.sh` and
  `flow/erc.sh`, all passing green with unchanged verdict fields).
- The SPEF-annotation legs of the sweep **cannot currently be re-run**:
  the operator-local `openroad` wrapper resolved to image
  `26Q3-2276-g4a7cf9b22a` on the verifying host, and under that OpenSTA
  build the annotation of this design's escaped-identifier nets regresses
  to incomplete (`partially_unannotated_driver_count: 40`,
  `delay_changed: false`, `annotation_complete: false` at the first
  corner -- the same escaped-identifier family as klayout-tools#1623 and
  #1624, resurfacing through a moved tool rather than a changed design).
  `flow/sta-sweep.sh` correctly refuses to record or update on that host,
  so the committed per-corner reports remain the recorded-toolchain
  evidence for the annotated legs. The LEF-only legs and the extraction
  itself run clean under the installed toolchain; the DEF and GDS inputs
  are byte-reproducible per above.
- This is toolchain drift of exactly the class `flow/README.md`'s
  "Toolchain versions" section documents (informational banner + diff
  authority), not a design change: nothing about the tile moved between
  the 2026-09-18 sweep and this verification.

## What this record does NOT claim

- **Not a re-ratification.** ADR-0002's ruling is untouched; this record
  re-meets its criteria against the post-PDN geometry.
- **Not an SDF-annotated simulation** -- that is the SDF record's own
  experiment (`flow/sdf-resim.sh`), whose successor record is
  `20260921-062530-e8a37ad`.
- **Not a hold-side ranking.** klt#1625 (no hold-side WNS/TNS in the
  per-corner reports) still applies; the recorded hold facts are the 0
  violation counts.
- **Not a claim that any Fmax number is ratified.** None is, per ADR-0002.
